function model = createModel(modelType, epsilon, beta, rho, Qinit)
    arguments
        Qinit = zeros(4,2);
        modelType = "Basic";
        epsilon = 0.1;
        beta = 1;
        rho = 1;
        
    end

    if strcmp(modelType, "Basic")
        model = Model(epsilon, rho, beta, Qinit);

    

